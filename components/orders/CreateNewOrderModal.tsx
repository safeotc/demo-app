import React from 'react';
import Modal, { ModalProps } from '../modal/Modal';

const CreateNewOrderModal: React.FC<ModalProps> = (props) => {
    return <Modal {...props}>TODO - create buy / sell tabs</Modal>;
};

export default CreateNewOrderModal;
